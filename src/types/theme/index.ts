export type Theme = {
  colors: {
    error: string;
    background: string;
    primary: string;
    primaryMedium: string;
    primaryBold: string;
    secondary: string;
    secondaryLight: string;
    green: string;
    greenLight: string;
  };
};
